import { SearchForm } from '@/app/components/search';

const SearchPage = () => {
   return (
      <div>
         <h1>Search for a Thing</h1>
         <SearchForm />
      </div>
   );
};

export default SearchPage;
